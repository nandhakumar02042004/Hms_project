services:
  - type: web
    name: hms-project
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn hms_project.wsgi:application"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
      - key: DJANGO_SETTINGS_MODULE
        value: hms_project.settings
